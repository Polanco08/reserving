<nav class="navbar navbar-default {% if currentpage == 'home'%} navbar-transparent {% endif %} navbar-fixed-top ">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="logo text-center" title="Reserving.me" href="/">
                <img src="{{ asset("app/images/logo.png") }}" class="img-responsive center-block" alt="Reserving"/>
            </a>
        </div>

        <div class="collapse navbar-collapse pull-right">
            <ul class="nav navbar-nav">
                <li class="main-menu__item {% if currentpage == 'home' %}{% endif %}">
                    <a href="{{ path("web_homepage") }}">Inicio</a>
                </li>
                <li class="main-menu__item {% if currentpage == 'servicios' %}active{% endif %}">
                    <a href="{{ path("web_servicios") }}">Servicios</a>
                </li>
                <li class="main-menu__item">
                    <a href="#planesPrecios">Planes y precios</a>
                </li>
                <li class="main-menu__item {% if currentpage == 'faq' %}active{% endif %}">
                    <a href="{{ path("web_preguntas_frecuentes") }}">faq's</a>
                </li>
                <li class="main-menu__item">
                    <a href="#contacto">Contacto</a>
                </li>
            </ul>
            <ul class="login-menu nav navbar-nav">
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <li class="login-menu__item login-menu__item--logout">
                        {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} |
                        <a href="{{ path('fos_user_security_logout') }}">
                            <i class="glyphicon glyphicon-log-out"></i> {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                        </a>
                    </li>
                {% else %}
                    <li class="login-menu__item login-menu__item--login">
                        <a href="{{ path('fos_user_security_login') }}">
                            <i class="glyphicon glyphicon-log-in"></i> {{ 'layout.login'|trans({}, 'FOSUserBundle') }}
                        </a>
                    </li>

                    <li class="login-menu__item login-menu__item--register">
                        <a href="{{ path("fos_user_registration_register") }}">
                            <i class="glyphicon glyphicon-user"></i> {{ 'layout.register'|trans({}, 'FOSUserBundle') }}
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>


<!-- home landing -->
<div id="landing-header">
    <div class="overlay">
        <div class="container">
            <div class="landing-header-content animated">
                <h1 class="content-box__title">
                    Gestiona tus Citas en línea.<br>
                    Utilizando nuestro <strong>programa de gestión</strong>
                </h1>
                <a class="btn btn-custom block-center" href="{{ path('fos_user_registration_register') }}">
                    <strong>Registrate</strong> y empieza a usarlo. !Gratis¡
                </a>
            </div>
        </div>
        <a href="#caracteristicas" class="anchor mas-info">
            <span>¿Que te ofrecemos?</span>
            <i class="glyphicon glyphicon-chevron-down"></i>
        </a>
    </div>
</div>

